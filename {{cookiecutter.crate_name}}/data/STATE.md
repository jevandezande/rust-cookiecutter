# State

Progress tracker for {{cookiecutter.project_name}}. `generate-spec` and `implement-spec` update
this file; `compare-code-to-spec` reads it. Keep it short: what is done, what is next, and where
the code departs from its design. Method-level detail belongs in `docs/methods/` and `specs/`.

## Current Focus

Nothing yet. Name the module under way, and its state in a sentence or two.

## Next Action

Nothing yet. Name the next concrete step, specific enough to start from cold.

## Modules

One row per module, in implementation order. `Spec Generated` is `n/a` for a module too small to
warrant a spec; tick a box once that phase is done and green.

| # | Module | Spec Generated | Code Implemented | Tests Passing |
| --- | --- | --- | --- | --- |

## Deviations

Anything built differently from its spec or method document, and why. Empty is the goal.
